# ROLE:
# Runtime dependencies for worker service.
#
# Used to execute deployment jobs.
# No API, no FastAPI, no HTTP.

pydantic>=2.6
pydantic-settings>=2.2

sqlalchemy>=2.0
psycopg2-binary>=2.9

httpx>=0.27

docker>=7.0.0
