[project]
name = "easy-deployer-hub-backend"
version = "0.1.0"
description = "Backend API for Easy Deployer Hub"
requires-python = ">=3.11"

[tool.black]
line-length = 100
target-version = ["py311"]

[tool.pytest.ini_options]
testpaths = ["../tests/backend"]
addopts = "-q"
